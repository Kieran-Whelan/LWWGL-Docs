import React from "react";
import CodeSample from "../CodeSample"

const text = 
`<div>
    <span>
    </span>
</div>`

function Docs () {
    return (
        <div>
            <CodeSample text = {text}/>
        </div>
    );
}

export default Docs; 